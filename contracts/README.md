# contracts

Versioned interfaces as files, not as code. Empty until the product is named.

When the first schema lands: JSON Schema or OpenAPI, semantic version in the filename, tests beside the file, generated clients never edited by hand. A path the OpenAPI describes must exist in the tree. `$id` is an identifier, not an address.

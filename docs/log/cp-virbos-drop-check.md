# CP-virbos-drop-check

- Changed: `factory/drops/virbos.md` now feeds a size step on Virbos's existing `tools/check.yml` plus an archive sitting. Morning F8 drop superseded (more law, no check). Intake I21 child both. Trap T52. Decision D-31. Register path is root `DECISION_REGISTER.md`. `BUSINESS_RULES.md` not capped. `SESSION_LOG.md` frozen and excluded.
- Measured: `node factory/tools/intake.mjs --check-register`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`; `node factory/tools/sitting.mjs check`.
- Self-check changed: did not overlay factory/ onto Virbos; did not grow Virbos live files; did not mint essay rows for Virbos's register; did not cap BUSINESS_RULES; did not unfreeze SESSION_LOG; did not use Otto YAML indent on check.yml.

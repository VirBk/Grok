# F10

- Changed: Cloud CP deploys a writer terminal. Isolates `git-bus` and `codespace`. Topology `cloud-git-bus`. `cloud-grok` is codespace+aider. `hands.mjs watch`. Cursor Cloud Agents are not a writer seat. Trap T44. Decision D-17. No GitHub Action. Live picks stay local-hands. F3 still queued on a key.
- Measured: `node factory/tools/hands.mjs --self-test`; `node factory/tools/hands.mjs watch`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`; `node factory/tools/landingGate.mjs --sha HEAD`.
- Self-check changed: topologies carry the new isolate ids. `watch` prints idle with no envelopes. Historical Actions were not added.

# F11

- Changed: `promptCache` is a PM pick. Native prefix cache (DeepSeek, Grok, Qwen, local KV). Factory orchestrates layout and pasted hits. No answer store. Trap T45. Decision D-18. Live pick `local-kv`.
- Measured: `node factory/tools/hands.mjs --self-test`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`; `node factory/tools/landingGate.mjs --sha HEAD`.
- Self-check changed: every topology carries `promptCache`. Unknown id fails `hands.mjs check`. Rates gained `inCached` as indicative, not copied into README.

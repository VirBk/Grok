# F12

- Changed: Token-only writer path. Claude Code absent is the default. Topologies `pc-token`, `pc-dashscope`, `cloud-dashscope`. `cloud-grok` stays the DeepSeek cloud path. Recipes and `factory/.env.example`. Trap T46. Decision D-19. Live picks stay local-hands. F3 still queued on the key.
- Measured: `node factory/tools/hands.mjs --self-test`; `node factory/tools/seat.mjs recipe pc-token`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`; `node factory/tools/landingGate.mjs --sha HEAD`.
- Self-check changed: new topologies pass catalog membership. No Claude Code binary is required. No GitHub Action added.

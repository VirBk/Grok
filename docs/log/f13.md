# F13

- Changed: Sidecar factory. `helpMode` pick. Topologies `help-fork` and `help-collab`. Isolate `help-clone`. `factory/help.example.json`. Trap T47. Decision D-20. Virbos stays alumni. Overlay still shelf. Live `helpMode` remains `none`.
- Measured: `node factory/tools/hands.mjs --self-test`; `node factory/tools/seat.mjs recipe help-fork`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`; `node factory/tools/landingGate.mjs --sha HEAD`.
- Self-check changed: `help-clone` prints a sibling clone and refuses missing `factory/help.json`. `hands.mjs check` still passes on the parent with `helpMode` `none`.

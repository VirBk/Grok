# F14

Owner-picked writer ladder. Meter is DashScope PAYG, not Token Plan.
Path auto: native DeepSeek off-peak if healthy; DashScope on peak or native silence.
Pin native or dashscope. Qwen is always DashScope.

Measured: `node factory/tools/route.mjs --self-test`

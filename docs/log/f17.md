# F17: DashScope DeepSeek Flash on the live seat

## What changed

Live pick is `deepseek-v4.1-flash` on the same `DASHSCOPE_API_KEY`. Path stays `dashscope`. Hosted recipes map `deepseek-flash` → `deepseek-v4.1-flash` and `deepseek-v4-pro` → `deepseek-v4-pro-0813`. Catalog gained those DashScope ids. T48. D-27.

## Measured

```
$ node factory/tools/seat.mjs recipe pc-dashscope
```

Hosted launch line: `qwen --auth-type openai --model deepseek-v4.1-flash`

```
$ node factory/tools/route.mjs resolve
```

```
family  deepseek
model  deepseek-v4.1-flash
requested  dashscope
path  dashscope
reason  pin-dashscope
base  https://dashscope-intl.aliyuncs.com/compatible-mode/v1
key  DASHSCOPE_API_KEY
```

```
$ node factory/tools/seat.mjs --self-test
self-test ok
```

```
$ node factory/tools/hands.mjs check
project picks ok
```

## Self-check

Seat self-test covers native-id leak. Route self-test covers hosted family + pin + key. `apply-topology pc-dashscope` would reset writerModel to `qwen3-coder-plus`; do not run it as part of this land.

# F19

Owner: setup the GitHub Action; VirBk/virbk is public.

Changed: `.github/workflows/landing.yml` runs `node factory/tools/landingGate.mjs --sha HEAD` on ubuntu-latest. No writer secrets. Trap T54. Decision D-36. Writer runner stays shelf.

Measured:

```
$ test -f .github/workflows/landing.yml && grep landingGate.mjs .github/workflows/landing.yml
        run: node factory/tools/landingGate.mjs --sha HEAD
```

```
$ node factory/tools/landingGate.mjs --sha HEAD
GATE PASSED
```

Self-check: workflow has no DASHSCOPE_API_KEY or DEEPSEEK_API_KEY. Linux runner is the named adversary. Did not add the writer runner.

# F20

Owner placed DASHSCOPE_API_KEY. Writer Action added.

Changed: `.github/workflows/writer.yml` plus `factory/tools/ghaWriter.mjs`. Watch idle. Issued envelope launches qwen-code. Pushes `writer/<lane>` only. Trap T55. Decision D-37. Writer runner off the shelf.

Measured:

```
$ node factory/tools/ghaWriter.mjs watch
idle
```

```
$ node factory/tools/ghaWriter.mjs --self-test
self-test ok
```

Self-check: landing.yml still names no writer secret. writer.yml maps secrets.DASHSCOPE_API_KEY. Source refuses main. Live envelopes empty, so first run is idle.

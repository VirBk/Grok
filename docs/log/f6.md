# F6

- Changed: Hands split. `factory/project.json` is the project manager pick. Catalog in `factory/runtimes.json`. Runner `factory/tools/hands.mjs` does not call a model. Envelopes are git blobs. Claude Code is optional. Default topology `local-hands`.
- Measured: `node factory/tools/hands.mjs --self-test` and `node factory/tools/landingGate.mjs` on this tree.
- Self-check changed: unknown harness fails; extra field fails; every named topology validates; no GitHub Action added.

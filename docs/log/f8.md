# F8

- Changed: Alumni drops for Otto and Virbos. `factory/drops/`. Lineage status alumni. Intake I18. Trap T41. Decision D-15. COPY refuses retrofit. Overlay is a later envelope.
- Measured: `node factory/tools/intake.mjs --check-register` and `--self-test`; `node factory/tools/spark.mjs --self-test`; `node factory/tools/sizeBudget.mjs`; `node factory/tools/renderBoard.mjs --check`.
- Self-check changed: drops name WHEN (O37; PK-3b and LEAVE-TYPE-KEY). Product holds left untouched. No GitHub fork. No factory overlay.

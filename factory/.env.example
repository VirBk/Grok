# Writer tokens. Copy into the seat env or a Codespaces secret.
# Never git. Never a VITE_ variable. Hands never read this file.
# DashScope PAYG (Singapore) — Qwen, and DeepSeek failover.
# DASHSCOPE_API_KEY=
# Native DeepSeek — cheaper off-peak. Optional if path is dashscope-only.
# DEEPSEEK_API_KEY=

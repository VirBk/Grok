ENVELOPE: F15 — Map hosted writer model to a real DashScope id
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make hosted DashScope launch lines use the live factory/project.json writerModel, not a hardcoded qwen3-coder.

Acceptance
  `node factory/tools/seat.mjs recipe pc-dashscope` and `recipe qwen-code` print `--model qwen3-coder-plus` (or whatever writerModel is live) on hosted launch lines. Hosted lines do not contain `--model qwen3-coder`. Local Ollama lines may keep qwen3-coder. Windows hosted stanza sets `$env:OPENAI_API_KEY = $env:DASHSCOPE_API_KEY` and `$env:OPENAI_BASE_URL` to dashscope-intl. Never echo a key value.

Base
  acc744d89cf636cff43e51a2a3713eca55803763
  Isolate from live main AFTER this envelope is on origin/main (`git ls-remote origin refs/heads/main`). Worktree from that SHA. `git checkout -b f15` before edits. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/seat.mjs
  docs/log/f15.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  Hands never read API keys. Keys never enter git. Never a VITE_ variable.
  Meter is DashScope PAYG Singapore. Hosted base is dashscope-intl.aliyuncs.com, not Beijing.
  Do not write factory/project.json, factory/runtimes.json, factory/board.json, AGENTS.md, or factory/envelopes/.
  Catalog id qwen3-coder is the local Ollama tag. It is not a DashScope API name.
  Do not install Claude Code. Do not remap the control-plane session.

Verification
  node factory/tools/seat.mjs recipe pc-dashscope
  node factory/tools/seat.mjs recipe qwen-code
  node factory/tools/seat.mjs recipe local-hands
  Hosted recipes contain the live writerModel and dashscope-intl. They do not contain `--model qwen3-coder`. Paste those three outputs in docs/log/f15.md.

Look hardest at
  Hardcoding qwen3-coder-plus instead of reading project.json, so the next owner pick is ignored.
  Changing local Ollama lines.
  Touching a control-plane hold to "be helpful."
  Echoing a key.

Runtime
  Topology pc-dashscope. writerHarness qwen-code. writerModel qwen3-coder-plus. isolate worktree. land ff-only.
  writerMeter dashscope-payg. writerPath dashscope. promptCache provider-prefix.
  Writer seat only. Hands: node factory/tools/hands.mjs. Never a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last. Paste cache hits. Do not store answers.

Spend cap
  USD 2 or 20 turns, then stop and return.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope. Never the transcript.

Self-check
  Drive the verification list. Confirm the diff is only the two holds. Record what the self-check changed in docs/log/f15.md.

Return
  A few lines in docs/log/f15.md: what changed, the measuring command output, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you.

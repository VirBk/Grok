ENVELOPE: F16 — Hosted recipes print the live DashScope model id
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Why this exists
  F15 split. qwen3-coder-plus escaped the JS template, so recipes printed the placeholder ${writerModel} instead of qwen3-coder-plus. Do not land F15.

Work
  In factory/tools/seat.mjs, hosted DashScope launch lines must print the live factory/project.json writerModel.

Wrong (do not ship):
  `#    qwen --auth-type openai --model \${writerModel}`
  That prints the six characters dollar-brace-writerModel.

Right (ship this):
  const writerModel = JSON.parse(readFileSync(join(root, "factory", "project.json"), "utf8")).writerModel;
  `#    qwen --auth-type openai --model ${writerModel}`
  With live writerModel qwen3-coder-plus, the printed recipe MUST contain the text qwen3-coder-plus.

Acceptance
  node factory/tools/seat.mjs recipe pc-dashscope
  node factory/tools/seat.mjs recipe qwen-code
  Those two outputs contain `--model qwen3-coder-plus` (the live id). They do not contain `--model ${writerModel}` as a placeholder. Hosted Windows lines use `$env:OPENAI_API_KEY=$env:DASHSCOPE_API_KEY` and dashscope-intl. Local Ollama lines keep the literal qwen3-coder. Import from node:fs. Never echo a key.

Base
  e2fb6fa97e86e736a2544b7df6c1c47f0cdc6fe4
  Isolate from live main AFTER this envelope is on origin/main. Worktree from that SHA. git checkout -b f16 before edits. Do not continue branch f15. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/seat.mjs
  docs/log/f16.md
  Changed file set equals this list.

Must stay true
  Hands never read API keys. Keys never enter git. Never a VITE_ variable.
  Meter is DashScope PAYG Singapore. Hosted base is dashscope-intl, not Beijing.
  Do not write factory/project.json, factory/runtimes.json, factory/board.json, AGENTS.md, or factory/envelopes/.
  Do not install Claude Code. Do not remap the control-plane session.

Verification
  node factory/tools/seat.mjs recipe pc-dashscope
  node factory/tools/seat.mjs recipe qwen-code
  node factory/tools/seat.mjs recipe local-hands
  If pc-dashscope does not contain qwen3-coder-plus, the work is not done. Paste the three outputs in docs/log/f16.md.

Look hardest at
  A backslash before ${writerModel}. That is the F15 miss. Grep the file for backslash-dollar before you commit.

Runtime
  Topology pc-dashscope. writerHarness qwen-code. writerModel qwen3.7-plus. isolate worktree. land ff-only.
  writerMeter dashscope-payg. writerPath dashscope. promptCache provider-prefix.
  Writer seat only. Hands: node factory/tools/hands.mjs. Never a model.

Spend cap
  USD 3 or 16 turns, then stop and return.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign.
  Successor reads AGENTS, BOARD, this envelope. Never the transcript.

Self-check
  Drive the verification list. Confirm the diff is only the two holds. If the recipe still prints a placeholder, do not push.

Return
  A few lines in docs/log/f16.md: what changed, the measuring command output, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you.

ENVELOPE: F3 — First cheap writer envelope
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Point DashScope writer recipes at the Singapore compatible-mode base, and add a Windows launch stanza.

Acceptance
  `node factory/tools/seat.mjs recipe qwen-code` and `recipe pc-dashscope` print `https://dashscope-intl.aliyuncs.com/compatible-mode/v1` as the hosted launch URL. They do not print `https://dashscope.aliyuncs.com/compatible-mode/v1` as a launch URL. Beijing may appear only as a "do not use for this meter" comment. A Windows stanza uses `$env:DASHSCOPE_API_KEY` and `$env:OPENAI_BASE_URL`, never `echo` of a key.

Base
  d4e75def9b444735993e99b523a22ce2d381a3c3
  Isolate from live main AFTER this envelope is on origin/main (`git ls-remote origin refs/heads/main`). Worktree from that SHA. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/seat.mjs
  docs/log/f3.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  Hands never read API keys. Keys never enter git. Never a VITE_ variable.
  Meter is DashScope PAYG Singapore (ap-southeast-1), not Token Plan, not Coding Plan, not Beijing dashscope.aliyuncs.com.
  Do not install Claude Code or Cursor to spend the token. Do not remap the control-plane session.
  Do not write factory/project.json, factory/board.json, AGENTS.md, or factory/envelopes/.
  Do not print a key, a key prefix, or a key length that is the key.

Verification
  node factory/tools/seat.mjs recipe qwen-code
  node factory/tools/seat.mjs recipe pc-dashscope
  node factory/tools/route.mjs resolve
  Recipes contain dashscope-intl.aliyuncs.com. Launch lines do not use dashscope.aliyuncs.com without -intl.
  route.mjs still exits 0. Paste those command outputs in docs/log/f3.md, redacting any secret.

Look hardest at
  A Singapore PAYG key against the Beijing host is a silent 401.
  `export` on Windows is not how the owner's seat sets env.
  A recipe that writes DASHSCOPE_API_KEY into a file.
  Installing Claude Code because qwen is missing.
  Touching a control-plane hold to "be helpful."

Runtime
  Topology pc-dashscope.
  writerHarness qwen-code. writerModel qwen3-coder. isolate worktree. land ff-only.
  writerMeter dashscope-payg. writerPath dashscope (native key absent; auto would also be DashScope).
  promptCache provider-prefix. access owner-only. commitCredit owner-account.
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Hands: node factory/tools/hands.mjs. Never a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last. Paste prompt_cache_hit_tokens or cached_tokens. Do not store answers.

Spend cap
  USD 2 or 6 turns, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope on a stronger seat.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope. Never the transcript. Never a compaction.
  A fresh same-tier instance beats a cheaper tired model finishing the job. On a decay sign: stop, print the signs, return.

Self-check
  Before returning, drive the verification list and fix what you find. Record in docs/log/f3.md what the self-check changed. Confirm the diff is only the two holds.

Return
  A few lines in docs/log/f3.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.

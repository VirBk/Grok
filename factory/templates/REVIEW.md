ENVELOPE: review of <LANE> — <TITLE>
Authorization: this envelope. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then read the writer's envelope. You test the lane against those, not against the writer's printed return.

Target <SHA> on branch <BRANCH>, parent <PARENT>. Detached worktree. You are the reviewer, never the author: edit nothing, commit nothing, push nothing.

Cover behaviour and coherence together:
  contracts, permissions, screens, rules, and every sentence of prose agree with the code
  the changed file set is the envelope's holds and nothing more
  every count is pasted from a command
  the suites the return names pass when you run them

Look hardest at whatever the writer envelope named.

The first line of your return is exactly VERDICT: APPROVED or VERDICT: CHANGES REQUESTED, alone. Then each finding with file and line, the sentence that makes it a defect, and what would close it. Then what you verified and how.

You run in print mode. Never end a turn waiting on a background task.
